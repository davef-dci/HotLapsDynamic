<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <cache-path
        name="cache"
        path="." />

    <!-- Keep only if you already rely on it elsewhere -->
    <external-path
        name="external_files"
        path="." />

</paths>
